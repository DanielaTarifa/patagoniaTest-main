spring:
  application:
    name: loan-service
cloud:
  config:
    enabled: true
    url: http://localhost:8004

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost