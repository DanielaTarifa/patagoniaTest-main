server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/DanielaTarifa/patagoniaTest-main.git
          search-path: config-data

  application:
    name: config-service
