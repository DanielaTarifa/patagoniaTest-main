spring:
  application:
    name: client-service
cloud:
  config:
    enabled: true
    uri: http://localhost:8001

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost